---
import black from "../../public/_dark.svg?raw";
import white from "../../public/_light.svg?raw";

// const isLightMode = document.documentElement.classList.contains("dark");
// const isLightMode = localStorage.getItem("theme") === "light";
const isLightMode = true;
---

<>
  {isLightMode ? <Fragment set:html={white} /> : <Fragment set:html={black} />}
</>
